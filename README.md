# Customer Segmentation — Czech Bank (Berka)

Сегментация банковских клиентов по транзакционной истории:
RFM-признаки считаются SQL-запросами в PostgreSQL, затем кластеризация
K-Means и бизнес-интерпретация полученных сегментов.

## Датасет

[Berka (PKDD'99)](https://www.kaggle.com/datasets) — реальные анонимизированные
данные чешского банка: ~4500 счетов, ~1 млн транзакций, 8 связанных таблиц
(клиенты, счета, транзакции, кредиты, карты).

Выбрал его по двум причинам:

1. **Реляционная схема.** RFM нельзя посчитать по одной таблице — придётся
   соединять клиентов, счета и транзакции. Главная цель проекта — прокачать
   SQL, и здесь JOIN'ы нужны по-настоящему, а не как упражнение.
2. **Финансовый домен.** Мой первый проект — [credit-risk-scoring]
   (https://github.com/rriizz2k/credit-risk-scoring) — про кредитный риск.
   Сегментация клиентов того же типа данных делает портфолио цельным
   и ближе к задачам финансовой аналитики.

## Стек

Python, PostgreSQL (Docker), SQL, pandas, scikit-learn, matplotlib, uv

## Пайплайн

1. **Postgres в Docker** — 8 таблиц Berka, загрузка через pandas `to_sql`
2. **RFM-признаки**:
   - Recency — SQL: `ROW_NUMBER()` по последней транзакции счёта, с фильтром
     служебных начислений процентов банка (`k_symbol LIKE '%UROK%'` искажал
     recency почти у всех клиентов — начисление на конец года выглядело как
     активность)
   - Frequency, Monetary — pandas, агрегация по клиенту
3. **Кластеризация** — StandardScaler + K-Means, число кластеров подтверждено
   elbow-методом (k=4)

## Сегменты


| Сегмент | Recency | Frequency | Monetary | Рекомендация                                                                                    |
| -------------- | ------- | --------- | -------- | ----------------------------------------------------------------------------------------------------------- |
| active         | 11      | 340       | 3.86M    | Ценные клиенты — удержать, VIP-программа                                     |
| usual          | 12      | 256       | 1.33M    | Стабильная база — предложить кредит/ипотеку                           |
| lowA-usual     | 14      | 107       | 630k     | Вероятно не основной счёт — выгодные условия для перехода  |
| inactive       | 574     | 27        | 59k      | Персонализированное предложение, собрать причины простоя |

## Ограничения

- Семейные счета (несколько владельцев на одном account_id) задваивают
  recency/frequency/monetary — метрики считаются на клиента, а не на счёт
- Датасет 1993–1998, кредитные операции не включены в RFM (только `trans`)
